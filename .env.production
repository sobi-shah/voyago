NODE_ENV=production
PORT=5000

# Provide your production MongoDB Atlas URI here
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/Voyago?retryWrites=true&w=majority

# Generate a strong, secure random string for production
JWT_SECRET=voyago_production_super_secret_key_change_this_before_deployment

# (Optional) Frontend URL for CORS if backend and frontend are hosted separately
# FRONTEND_URL=https://voyago.pk
